<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Dukandar20" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorControlNormal">@color/emeraldGreen</item>
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Dukandar20" parent="Base.Theme.Dukandar20" />

    <style name="roundedImageView" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">9dp</item>

    </style>
    <!-- In res/values/styles.xml -->
    <style name="CustomSearchViewStyle">
        <item name="android:queryHint">"Search"</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:editTextColor">@color/emeraldGreen</item>
        <item name="android:textColorHint">@color/black</item>
        <item name="android:searchIcon">@drawable/search24green</item>
                                                                       <item name="android:closeIcon">@drawable/search24green</item>
    </style>

</resources>